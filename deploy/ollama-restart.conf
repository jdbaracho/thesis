# Drop-in for ollama.service. Install to
# /etc/systemd/system/ollama.service.d/restart.conf only if
# `systemctl show ollama -p Restart` does not already report Restart=always.
[Service]
Restart=always
RestartSec=5s
